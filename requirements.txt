# Claude Max 구독으로 호출 — Claude Code CLI(`claude`)가 설치되어 있고
# `claude /login` 으로 Max 계정에 로그인되어 있어야 함.
claude-agent-sdk>=0.1.0

# 무료 TTS (Microsoft Edge TTS, 한국어 매우 자연스러움)
edge-tts>=6.1.0

# 텔레그램
python-telegram-bot[ext]==21.10

# 스케줄링
APScheduler==3.10.4

# 구글 통합 (선택)
google-api-python-client==2.151.0
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# 유틸
python-dotenv==1.0.1
PyYAML==6.0.2
pytz==2024.2
httpx>=0.27.0
